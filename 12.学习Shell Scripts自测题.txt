12.3 善用判断式
1.利用test来检测/dmtsai 是否存在，如果存在显示“exist”，不存在显示“Not exist”
2.判断file文件是否存在且为“非空白文件”？
3.判断file1是否比file2旧？
4.如何判断两个文件是否指向同一个inode？
5.判断n1和n2不相等？
6.判断字串为空，为空则返回true
7.判断字符串为非空，为空返回false
8.判断str1和str2是否相等
9.如何判断file同时具有读写权限？
10.如何判断file不具有可执行权限的时候回传true
11.让使用者输入一个文件名，判断这个文件名是否存在，如果不存在则输出“Filename
   does not exist”并中断程序；如果存在则继续判断是文件还是目录，对应的输出
   “Filename is a regular file”或“Filename is directory”；最后，判断执行
   者的身份对这个文件或目录所拥有的权限，并输出权限信息
