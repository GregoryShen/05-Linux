10.2.3 环境变量的功能
1. 如何查看当前shell环境的默认环境变量（两种方法）
2. 如何查看当前shell环境的所有变量？（两种方法）
3. 如何查看命令提示符的设定？
4. 请利用环境变量RANDOM随机生成一个0~9之间的随机数
5. 如何查看本shell的PID
6. 如何查看上一个指令的回传值，什么数值代表上个指令执行正确/错误？
7. 环境变量与自定义变量的区别是什么？
8. 如何把一个自定义变量转换为环境变量？

10.2.6 变量键盘读取、数组与声明：read，array, declare
1. 提示使用者30秒内输入自己的大名，将该输入字符串作为名为named 的变量内容
2. 让变量sum 进行100+300+50的加总结果
3. 将变量sum 变成环境变量，查看
   再将sum 变回成自定义变量，查看

10.2.7 与文件系统及进程的限制关系：ulimit
1.列出你目前身份（假设为一般账号）的所有限制数据数值
  如果数据是0代表什么，解释file size和 open files 的含义
2.限制使用者仅能建立10MBytes 以下容量的文件并进行验证
3.怎样恢复ulimit的默认限制？

10.2.8 变量内容的删除、取代与替换
1. 假设你是dmtsai，那你的MAIL变量应该是 /var/spool/mail/dmtsai。假设只想要保留最后的文件名，如何实现？
2. 承接1，如果只想去掉文件名保留目录呢
3. 将path的变量内容中的sbin 全部取代成大写SBIN
4. 测试系统中是否有username这个变量，如果username没有设定或为空字符串，将username的内容设为root


10.5.1 什么是数据流重导向
1.标准输入，标准输出和标准错误输出的代码是多少，使用什么符号表示（输入输出4种，错误输出2种）？
2.>和>>有什么区别,
  2>和2>>有什么区别
  <和<< 有什么区别
3.用一般账号搜索/home下是否有名为.bashrc的文件，并将正确的信息保存到list_right文件，
  丢弃错误信息
4.将2中的信息都输出到list文件中
5.如何利用cat指令来建立一个文件（文件名为catfile）？并使用eof作为结束输入符
6.假设我要将echo "error message"以standard error output 的格式来输出，该怎样做？
  而且要验证所输出的结果确实是到标准错误输出了

10.5.2 命令执行的判断依据：;,&&,||
7. 我想要在/tmp/abc 下面建立 /tmp/abc/hehe 这个文件，但是不确定/tmp/abc是否
存在，如果不存在的话也一并建立，怎么操作？
8. 以 ls 測試 /tmp/vbirding 是否存在，若存在則顯示 "exist" ，若不存在，則顯
示 "not exist"！


10.6.6 参数替换： xargs

1. 将 /etc/passwd 内第一列取出，仅取三行，使用id将每个账号内容展示出来

2. 同上，但是每次执行id时，都询问使用者是否动作？

3. 将所有的 /etc/passwd 内的账号都以id 查阅，但查到sync就结束指令串

4. 用两种方法来找出 /usr/sbin 底下具有特殊权限的文件名，并使用ls -l 列出详细属性

5. 为什么要使用xargs？当xargs后面没有接任何指令时，默认是以什么来进行输出？


